本段程序基于selenuim和chrome driver编写，win上的可执行文件为webdriver.exe，源代码为webdriver.py，用pyinstaller封装，运行时电脑必须装有Chrome浏览器（可能版本得在74以上，因为我用的driver版本就是74），也可以自己安装python运行环境执行代码。
由于时间仓促，没有做图像化界面封装，用原始的DOS模式，有时间再更新。
PS：由于评教速度较快，服务器可能会封号（我本人血的教训-_-），但是一两次没有关系，现在这个版本没有做延时处理，源码中有延时函数，但被注释掉了。
